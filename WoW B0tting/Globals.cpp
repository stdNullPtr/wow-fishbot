#include "Globals.h"

namespace g
{
    bool isBobberFound{ false };                 // fishing bobber found flag
    RECT wowWindowRect{};                      // wow window Rectangle struct
    long splashesCnt{ 0 };
    long didntHearSplashCnt{ 0 };
}